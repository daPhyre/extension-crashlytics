<?xml version="1.0" encoding="UTF-8"?>
<project name="ExtensionCrashlytics" default="help">

    <property environment="env"/>
    <property name="sdk.dir" value="${env.ANDROID_SDK}"/>

    <property file="local.properties"/>
    <property file="ant.properties"/>
    <property file="project.properties"/>

    <property name="crashlytics.project.path" location="../../"/>
    <property name="crashlytics.manifest.path" location="AndroidManifest.xml"/>

    <import file="fabric/crashlytics_build.xml"/>

    <import file="${sdk.dir}/tools/ant/build.xml"/>

</project>
